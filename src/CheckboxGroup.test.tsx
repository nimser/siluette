import { render, screen } from "@testing-library/react"
import CheckboxGroup from "./CheckBoxGroup"

describe("CheckboxGroup component", () => {
  it.skip("should set max amount of choices based on props", () => {
    //
  })
})
